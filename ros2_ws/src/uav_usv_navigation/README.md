# uav_usv_navigation

Ownership: navigation team.

Future contents:

- Nav2 parameters and launch files.
- Global route planning, MPPI, maritime costmap layers, goal tools, and Collision Monitor.
- Input from localization and fused tracks; output `/cmd_vel`.

The active Nav2 stack remains in `uav_usv_sim`.

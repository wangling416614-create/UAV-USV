# uav_usv_usv_control

Ownership: USV control team.

Future contents:

- `/cmd_vel` execution, speed and heading PID, command timeout, and emergency stop.
- Future left/right thruster allocation and vessel dynamics control.

The active Gazebo Twist and PID implementation remains in `uav_usv_sim`.
